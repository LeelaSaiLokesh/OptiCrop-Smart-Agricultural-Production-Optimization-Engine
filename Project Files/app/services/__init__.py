"""
OptiCrop – app/services/__init__.py
Services Package

Modules:
  prediction.py — PredictionService
                  Phase 13: Returns structured demo/placeholder result_data
                  Phase 14: Loads model.pkl, runs Random Forest inference,
                            returns live crop recommendation

Future additions (Phase 14+):
  model_loader.py  — Loads and validates model.pkl / encoder.pkl
  preprocessor.py  — Applies feature scaling pipeline before inference
"""
